#!/usr/bin/env bash
current=`pwd`
basename=crystal-$1-$2
tar=$basename.o.tar.gz
o=$basename.o
cd build
id=$(docker create mosop/crystal-bins-$2:$1)
docker cp $id:/$o $o
docker rm -v $id
rm -f $tar
tar zcvf $tar $o
rm $o
cd $current
